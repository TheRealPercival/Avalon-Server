{
  "name": "avalon-server",
  "version": "1.0.0",
  "description": "A websocket server",
  "license": "MIT",
  "author": "bkey3125@gmail.com",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "build": "tsc",
    "rebuild": "rm -rf dist && rm tsconfig.tsbuildinfo && tsc",
    "dev": "node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0",
    "dotenv": "^17.4.2",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^7.0.2"
  }
}
